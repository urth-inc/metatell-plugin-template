{
  "name": "custom-nearest-user-profile-template",
  "description": "A template for creating custom nearest user profile for metatell",
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "dev": "npm run set-uuid && env-cmd -f .uuid.env vite",
    "build": "rimraf dist && npm run set-uuid && env-cmd -f .uuid.env vite build && env-cmd -f .uuid.env node ./scripts/add_metadata.js && node ./scripts/archive_dist.js",
    "preview": "vite preview",
    "lint:tsc": "tsc --noEmit",
    "lint:biome": "biome check src",
    "lint": "npm run lint:tsc && npm run lint:biome",
    "lint-fix": "biome check --write src",
    "set-uuid": "node ./scripts/set_uuid.js > .uuid.env"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.13",
    "@module-federation/vite": "^1.9.4",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^5.1.2",
    "adm-zip": "^0.5.16",
    "env-cmd": "^10.1.0",
    "rimraf": "^6.0.1",
    "sass": "^1.81.0",
    "typescript": "^5.5.4",
    "uuid": "^13.0.0",
    "vite": "^6.4.1"
  },
  "dependencies": {
    "classnames": "^2.5.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "volta": {
    "node": "24.11.0",
    "npm": "11.6.2"
  }
}
